fn main() {
    mut sum = 0;
    mut odd_sum = 0;
    for i in rev 0..5 unroll {
        sum = sum + i;
        remainder = i % 2;
        if remainder == 1 {
            odd_sum = odd_sum + i;
        }
    }
    assert sum == 10;
    assert odd_sum == 4;
    return;
}
