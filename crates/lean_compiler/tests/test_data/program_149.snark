// Test push with nested vectors
fn main() {
    v = vec![vec![1, 2]];
    assert len(v) == 1;
    v.push(vec![3, 4]);
    assert len(v) == 2;
    assert v[1][0] == 3;
    assert v[1][1] == 4;
    v.push(vec![5, 6, 7]);
    assert len(v) == 3;
    assert len(v[2]) == 3;
    assert v[2][2] == 7;
    return;
}
